apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: floor-control
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: floor-control
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
