apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-service.yaml
  - api-deployment.yaml

images:
  - name: floor-control-api
    newName: floor-control-api
    newTag: kind-local
